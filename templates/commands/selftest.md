---
description: Run this project's health check and report the result
---

Run this project's health check: `sonelle.check.ps1`.

- If it passes (exit 0), confirm in one line.
- If it fails, show what failed and propose the smallest fix - diagnose only, do not edit yet.
- If it reports a placeholder (exit 2), the project has no real checks yet - point that out.
