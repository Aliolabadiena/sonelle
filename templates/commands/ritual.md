---
description: Run the end-of-task ritual (state + capture the lesson)
---

Close out this task:

1. Update the project's TODO (mark items done with a short note) and ledger (what changed, new gotchas,
   and exact RESUME steps if anything is unfinished).
2. If you learned something reusable, capture it via `tools\log_lesson.ps1` so it is not lost.
3. Confirm `sonelle.check.ps1` still passes.
