---
description: Run the health check and fix every failure until it passes
---

Heal this project:

1. Run `sonelle.check.ps1`.
2. For each failure, find the root cause and fix it - do not paper over it.
3. Re-run until it passes (exit 0).
