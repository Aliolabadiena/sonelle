---
description: Verify, update state, then commit this project
---

Ship the current change:

1. Run `sonelle.check.ps1` - it must pass. If it fails, stop and fix first; never commit on red.
2. Update this project's TODO (mark done) and ledger (what changed, any new gotcha).
3. Stage and commit with a clear message describing what changed and why.
4. Do NOT push unless I explicitly ask.
