---
description: Run the engine self-test and report PASS or the failing lines
---

Run the engine self-test: `tools\selftest.ps1`.

- If it ends with `[selftest] ALL PASS`, confirm that in one line.
- If anything is red, show each `[FAIL]` line and name the smallest fix - diagnose only, do not edit yet.
